import type React from "react"
import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Eigen Ontwerp Laser Snijden | Upload DXF — FORMD",
  description:
    "Upload je DXF-bestand en krijg direct een prijs voor lasersnijden op maat. Staal, RVS, aluminium, messing en chroom. Instant prijsberekening met 3D-preview.",
}

export default function EigenOntwerpLayout({ children }: { children: React.ReactNode }) {
  return children
}
